# Mobile Prototype Agent Guide

## Prototype Instructions

## Product-Specific Direction

- This is a standalone interview MVP for the “驭小满” Tianjin annual-inspection service and is unrelated to the medical repositories under `E:/workspace`.
- Product decision (2026-08-22): the React/Vite interview MVP is frozen and is no longer a maintained product surface. Keep its existing source files for historical/demo reference, but do not synchronize new features into it, change it as part of ordinary feature work, or include its runtime, build, Sites, or Playwright checks in feature acceptance. Unless the user explicitly reopens that surface, all new product work targets only the native WeChat mini-program, the operations admin, and their shared Fastify/PostgreSQL API.
- Product decision (2026-08-22): “补贴咨询” is a generic policy-and-material consultation journey, never an invoice, subsidy-application, subsidy-eligibility, or outcome-guarantee product. Operations may freely add, remove, reorder, edit, disable, save, preview, and publish owner-value consultation-fee tiers up to ¥500,000; the server alone resolves a 10-minute quote and freezes its plan version on submission, while the client cannot author the fee. The owner flow takes no payment, labels vehicle value as owner self-reported and non-appraisal, and treats vehicle photos only as consultation references. Its nine identity/vehicle materials are private, individually read-and-audited, non-exportable, owner-bound, and fail closed to synthetic demo data until production owner identity, HTTPS, encryption, restricted CORS, private storage, and a trusted admin subject are all configured.
- Product decision (2026-08-27): each subsidy-consultation fee plan also carries three operations-maintained administrative-charge reference items: plate handling ¥120, mailing ¥20, and document-production cost ¥10 by default. The owner-facing reference total is the server-frozen consultation service fee plus these administrative items, so the default four totals are ¥300/¥400/¥500/¥600. Freeze the itemized amounts, administrative subtotal, and reference total in every quote and submitted consultation; later configuration changes must not rewrite history, and the client may never author any component or total. Label the result only as “过户费用合计（参考）”, state that actual charges depend on the handling institution and selected services, and never describe any component as a tradable ticket/invoice, official fee, subsidy amount, or payment collected by this module.
- Product decision (2026-08-19): the current build is a presentation-stage product demo for investors and company leadership. Prioritize a short, deterministic, visually legible path that demonstrates the system's major capabilities and closed service loop. Key result states and downstream actions must be easy to reach with clearly labelled synthetic demo vehicles or scenario presets. Demo conveniences may prefill or select synthetic scenario data, but must be visibly identified as demo behavior and must not silently weaken the production-facing policy, ownership, pricing, or authorization rules.
- Product decision (2026-08-29): the repair-quote MVP is a native-mini-program and shared-API closed loop that begins only from an owner-visible, published vehicle-checkup report containing at least one recorded fault. Creating a request imports all report faults and their existing photos as an immutable request snapshot after one explicit scope-and-consent confirmation; there is at most one active request per report. The three clearly labelled synthetic demo repair shops remain demo subjects, but their staff surface is now account-backed: each shop has one named `repair_shop_admin`, enters through Profile → Staff Access → Repair Shop, receives a revocable `yxm_bo_` Bearer session bound to exactly one `repair_shop`, and can never select or author a shop ID. All active shops may view the sanitized open marketplace and the media needed to quote, while each may read or mutate only its own quote; after mock payment only the selected shop may continue to the deal details and authorized contact. Old Profile → Demo Tools role switching and anonymous `/api/demo/repair-shops/:shopId/*` authority are removed. Each shop may submit one full-repair total plus one short scope note; the owner compares price-first cards, selects one quote, and completes an explicitly labelled full mock payment. Payment must be transactional and idempotent, freeze the selected quote/shop/report snapshot, and never imply real money movement. Scheduling, vehicle handoff, repair fulfillment, acceptance, refund, settlement, chat, and offline disputes remain between the parties and outside this MVP. Preserve server-resolved owner isolation, repair-shop scoping, private media access, named-account revocation, and visible synthetic/demo boundaries throughout.
- Product decision (2026-08-26): the unified named-account backoffice supports platform administrators, invited wash-store administrators, and invited inspection-station administrators. Production must require a valid, revocable backoffice session before any `/api/admin/*`, `/api/operator/*`, private backoffice media, aggregate, or mutation is served. Platform administrators retain global scope. A wash-store administrator is bound to exactly one store and an inspection-station administrator to exactly one station; each receives only its fixed capabilities, and the server must scope lists, totals, details, reports, media, evidence, slots, and writes to that subject. Cross-subject resource IDs return the same `404`; hiding a menu or accepting a client-authored subject ID is never authorization. The native mini-program station surface exchanges the same named inspection-station credentials for a revocable `yxm_bo_` Bearer session through `/api/operator/sessions`; only an `inspection_station_admin` with an active station subject may use Bearer auth on `/api/operator/*`, while platform and wash administrators remain Cookie-only. The only anonymous fallback is the explicit test-only platform principal, which production ignores.
- Bootstrap the first platform administrator only with `npm run backoffice:create-admin` and an explicitly injected 12+ character password; never add a default credential. Synthetic demo wash stores and inspection stations must not receive accounts automatically. Platform administrators invite each service subject explicitly, and replacement activation must atomically switch the binding, disable the previous administrator, and revoke its sessions.
- Product decision (2026-08-26): the native inspection-station surface authenticates with a revocable `yxm_bo_` Bearer issued only after an invited `inspection_station_admin` signs in. It must never reuse an owner token or accept a platform/wash account. Every operator JSON request, upload, and private-media download carries that station Bearer; `401` clears it and returns to station login, while cross-station resources remain indistinguishable `404`s. Browser backoffice sessions remain HttpOnly Cookie based and retain HTTPS Origin checks; a native non-Cookie station Bearer may bypass only the browser CSRF Origin requirement, never capability or station scope.
- Product decision (2026-08-20): annual-inspection and car-wash appointment availability is cut off by the slot start time in `Asia/Shanghai`. A slot whose date is before the current business date, or whose date is the current business date and `startTime <=` the current Shanghai time, has started and is no longer bookable. Owner-facing lists and controls must not offer it as selectable, and the server must independently reject it during quote, booking creation, and rescheduling so stale clients cannot bypass the cutoff.
- Product decision (2026-08-28): the operations “预约/履约” list must show the immutable booking `createdAt` as a separate “下单时间” column rather than conflating it with the appointment date/time, and it must provide a server-backed vehicle-plate filter. Normalize case, full-width characters, spaces, and common plate separators for matching against the plate displayed from the frozen booking snapshot with a current-vehicle fallback; filtering must preserve the existing authenticated backoffice scope and use the same predicate for every returned item/count contract.
- The consumer-home visual source of truth is `references/selected-owner-home-v2.png` (the cohesive cool blue/white direction with a Tianjin skyline, white sedan, cobalt primary action, orange due-soon accent, four-stage annual-inspection guide, 3 × 2 owner-service grid, and the persistent Home / Orders / Profile navigation). Operator screens use the two operator references listed below while inheriting the same cool blue/white brand tokens.
- Product decision (2026-08-24): replace the owner home's four-step annual-inspection guide with three parallel annual-inspection jobs in the existing cool blue/white visual language: two equal cards for `自驾验车` and `代驾验车`, followed by one full-width `检测报告` status card. A bookable vehicle skips the service-mode page and enters the selected station flow directly; no-vehicle, eligibility-conflict, and active-booking cases preserve the chosen mode while routing to the required resolution or existing order. Booking photos remain on the booking-confirmation page. The report entry always opens an all-vehicle report center that keeps in-progress fulfillment and published report history separately discoverable; it never conditionally jumps straight to one latest report. The native booking submit must carry the server-issued `quoteSnapshotId`. Valet map selection must be reverse-resolved and signed by the server before it enters the draft, and valet pricing/payment must continue to fail closed unless Tencent returns a real driving route; map timeouts are configurable rather than fixed below observed provider latency.
- Preserve a focused annual-inspection journey: vehicle profile → eligibility and materials → station/time selection → booking → fulfillment progress → result.
- Product decision (2026-08-19): the consumer query funnel should help owners who actually need onsite inspection continue into station booking without biasing the policy result. Do not silently default the five special-condition declarations to `no`, and do not render untouched declarations as if the user actively selected `unknown`. Offer an explicit one-tap confirmation that none of the listed conditions applies; expand the five detailed questions only when the owner reports a matching condition or uncertainty. An unresolved calculation must lead back to resolving the missing declarations with specific guidance instead of ending at a generic official/manual-verification dead end.
- Product decision (2026-08-17): annual-inspection results must expose a five-step, source-linked policy derivation instead of generic reasons. Powertrain never changes the inspection cycle; it only changes applicable onsite items. Treat non-plug-in hybrid (`hybrid` / HEV) as a first-class vehicle powertrain and keep it distinct from PHEV/EREV. Rule-estimated dates and user-confirmed 12123/driving-license validity months must remain visibly distinct; never imply a live government sync, and disable ordinary booking when those months conflict.
- Product decision (2026-08-17): never prefill a user-confirmed inspection-validity month or source from a policy estimate, legacy date, or internal placeholder. Month and source must both be actively selected and submitted through an explicit compare/confirm action; opening or editing a draft is not confirmation. Preserve the original confirmation time when unrelated vehicle fields change, and keep booking blocked until a date conflict is explicitly resolved or the user returns to an unconfirmed policy estimate.
- Product decision (2026-08-17): consumer root navigation has exactly three destinations: Home, Orders, and Profile. Keep that navigation visible only on those root screens; pushed annual-inspection, wash, insurance, car-rental, vehicle, payment, and order-detail screens use their own header and do not show the root tab bar.
- Product decision (2026-08-23): the native mini-program main package registers only `pages/home/home`, `pages/orders/orders`, and `pages/profile/profile`, plus genuinely cross-domain services/types/utilities and assets required by those roots. Every non-Tab business journey belongs in a normal (not independent) domain subpackage and may reuse main-package shared code; domain-private utilities and assets travel with their subpackage, and main-package code must never import subpackage code. `npm run check:main-package` is a required CI guard: conservative main-package size must remain at or below 1.5 MiB, every package below WeChat's 2 MiB per-package hard limit, and all packages together below 20 MiB.
- Product decision (2026-08-17): annual-inspection station cards and station details must label `serviceFeeFen` as a reference price, never as a precise vehicle quote or a guaranteed lowest price. The booking-confirmation quote is the first exact vehicle-specific price. When controlled pricing cannot produce exactly one match, do not promise a nonexistent manual-review workflow; stop online quoting and direct the owner to the existing customer-service entry. Admin `sortOrder` is labelled as back-office ordering because it does not resolve overlapping rules.
- Keep “立即预约” and “查询是否需上线” as different jobs. Immediate booking enters the station/service-mode booking journey, while the query entry opens the rule-based annual-inspection calculation and handling advice; do not route both actions to the same screen.
- Each of the four annual-inspection stage icons opens a dedicated contextual page for booking, arrival, materials, or result. Derive its state from the selected vehicle and latest annual-inspection booking. With no applicable booking, show an honest preparation/empty state and the correct next action. The result stage may display only a backend-provided `inspectionResult` plus official 交管 12123 guidance; never fabricate an electronic inspection mark, government status, approval, or regulator integration.
- Preserve the two-sided service loop: the consumer side owns vehicle, eligibility, booking and result viewing; the station side owns the work queue, arrival verification, inspection handoff and result acknowledgement. The visible demo role switch explains this closed loop but grants no authority; consumer resource ownership still comes from the server-resolved application session.
- Keep the mini program/service platform and industrial inspection-line software as separate systems. Their only intersection is a versioned business handoff: the mini program sends an accepted booking/vehicle context outward and receives a signed inspection result back. Never add fake device telemetry, live charts, calibration controls, brake/exhaust equipment controls, or regulator-system controls to make the demo appear more capable.
- Use `references/operator-workbench-visual-reference.png` as the operator workbench/list visual source of truth and `references/operator-booking-detail-visual-reference.png` as the single-booking fulfillment detail source of truth. Keep `references/selected-owner-home-v2.png` as the consumer home visual source of truth.
- Product decision (2026-08-22): the inspection-stage vehicle checkup report borrows only the spatial depth and fault-locator interaction from `references/vehicle-checkup-tech-reference.png`. Implement it as an interactive, high-depth 2D multi-view vehicle locator (top, left, and right) with real raster vehicle assets, localized fault glow, numbered hotspots, a focused callout, and a synchronized fault list. Do not claim free-rotation 3D scanning, automatic damage recognition, device telemetry, or inspection-line diagnostics; the station operator is manually recording vehicle condition. Render the locator as a light ice-blue/cool-blue module using the existing card, radius, typography, spacing, border, and cobalt action tokens across operator, owner, and admin surfaces; avoid a visually disconnected black HUD.
- Product decision (2026-08-23): every recorded vehicle-checkup fault requires one to three station-authored close-up photos before an inspection result can be submitted. Draft reports may temporarily contain faults without photos, but the operator UI and server must both block result publication until every fault has at least one successfully stored image. Fault close-ups belong to exactly one stable fault ID, remain separate from the fixed five site-condition photos and the annual-inspection-mark image, and must be shown with that fault in the operator preview, owner report, admin report, and print output. Saving or reordering a draft must never recreate fault IDs or detach, replace, or cross-associate another fault's photos.
- All vehicle, station, pricing, policy, rating, operator, verification, inspection-result, and photo content is synthetic demo data. Do not imply live government, payment, OCR, map, inspection-device, regulator, or inspection-station integration.
- Product decision (2026-08-16): the Web MVP now contains one verified self-operated station, `station-huayang-1` / 天津市华洋机动车检测有限公司, and may use the backend-only Tencent Location Service key for real route quotes. Keep every other synthetic station visibly labelled as demo data, distinguish verified station identity/address from operator-configured prices/slots/ratings, and never present an estimated route as Tencent data.
- Product decision (2026-08-16): valet service means a round trip to the selected station and back to the original pickup address. Price it from the one-way Tencent driving distance; the return leg is included and is never charged a second time. The default price may be inherited globally or wholly overridden per station, and the service radius supports an explicit unlimited state.
- Product decision (2026-08-27): annual-inspection valet fulfillment uses a hidden, role-isolated `driver` subpackage inside the existing native WeChat mini-program rather than a second App or mini-program. Drivers enter through the fixed `我的 -> 工作人员入口 -> 代驾端` surface and type the operations-generated six-digit code for exactly one assigned order. An unbound code is valid for 24 hours; first successful exchange binds it to that verified WeChat user, who may reuse the same code to obtain a fresh task-scoped session until completion. Operations may view the plaintext code only in the single-order detail before completion; persist only encrypted plaintext plus a keyed lookup digest, rate-limit failed exchanges by user and source IP, and never expose the code to owner, station, driver-task, or admin-list DTOs. Keep the legacy opaque link/QR exchange compatible, but do not make it the normal demo path. The driver surface owns only pickup evidence, return-trip start, and final-return evidence. Do not add driver task pools, scheduling, tracking, chat, settlement, wallets, customer confirmation, or dispute features.
- Product decision (2026-08-19): mock payment must still exercise real financial behavior. Synthetic annual-inspection orders use non-zero, vehicle-appropriate fee snapshots, booking ledger charges, confirmed mock payment rows, and auditable paid/refunded totals. Owner, station, and admin surfaces must keep a persistent notice that no real SDK or money movement occurs; never use zero-value placeholders merely to demonstrate a paid state.
- Product decision (2026-09-02): a confirmed annual-inspection payment atomically advances the canonical fulfillment state from `pending_payment` to `pending_precheck`. The booked inspection station must review the seven owner-authored booking photos before physical fulfillment begins. Approval is the station's acceptance action: self-drive advances directly to `awaiting_arrival`, while valet advances to `confirmed` so operations may arrange the driver. Rejection records a structured reason, terminates fulfillment as `precheck_rejected`, releases slot capacity, and starts an idempotent full mock refund. Keep `paid_pending_confirmation` only as a backwards-compatible recovery state for historical data, not as the normal customer journey.
- Product decision (2026-08-20): annual-inspection document and vehicle-angle uploads are cumulative by media kind. Returning from the real-device camera or album must never reinitialize the booking page or discard previously uploaded slots; any explicit refresh must reconcile by kind and preserve successful or in-flight uploads. Re-selecting one kind may replace only that kind.
- Product decision (2026-09-02): both self-drive and valet annual-inspection booking require the same seven owner-authored photos before payment and station precheck: vehicle front-left, front-right, rear-left, rear-right, the dashboard after the vehicle is started, and both driving-license pages. The later valet pickup/station/return evidence remains a separate physical handoff record and does not replace these booking precheck materials.
- Product decision (2026-08-25): new valet annual-inspection orders use versioned, evidence-gated handoffs. The assigned driver captures five live-camera photos (four corners plus started dashboard) before `driver_arranged -> picked_up`; the station captures the same five before `picked_up -> checked_in`; the published vehicle-checkup report's existing fixed five photos are referenced, not copied, as the inspection-complete/station-release evidence; and the assigned driver captures five before `returning -> completed`. Completing an evidence package and advancing its state must be one idempotent transaction. Owners and platform administrators can view the full stage timeline and completed evidence, while customers never confirm, reject, or dispute a handoff. Historical orders keep their legacy policy and show missing historical evidence honestly rather than fabricating it.
- Treat mainland vehicle-plate entry as a product-domain component, not a generic text field. Keep the dedicated plate keyboard, explicit province selector, and visible character slots whenever vehicle add/edit is changed.
- Preserve the plate-mode contract: conventional blue plates contain 7 characters including the province abbreviation; new-energy green plates contain 8. Small new-energy vehicles place `D`/`F` in the first energy-code position, while large new-energy vehicles place `D`/`F` at the final position. Switching plate mode or vehicle size must clear any characters that are no longer valid instead of silently reinterpreting them.
- Plate controls must remain testable and accessible through stable roles, labels, and `data-testid` hooks. Add/edit hydration, province choice, slot focus, blue/green switching, `D`/`F` placement, validation, and deletion are interview-critical behavior, not decorative UI.

In ChatGPT Work Mode, run `sites-preview start "$PWD"`, open `http://terminal.local:4173/` in the cloud browser, and verify the rendered app and its primary interactions. Keep that preview open and tell the user to inspect it in the cloud browser; do not present the local URL as a user-facing chat link. In Codex Desktop, run the local server yourself, open the preview in the in-app browser, and provide the clickable local URL. Do not deploy to Sites unless the user explicitly asks to share, publish, or deploy. Do not give the user server-start instructions when you can run it.

Before planning or implementing any mobile-app change, read this `AGENTS.md` in full. It is the source of truth for the template's runtime and component guidance.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Editing Boundary

- Build app-specific UI in `src/Prototype.tsx` and `src/prototype.css`.
- Treat `src/App.tsx`, `src/main.tsx`, `src/styles.css`, `src/mobile/`, `public/assets/iphone/`, `public/assets/android/`, `public/assets/status/`, `vite.config.ts`, `worker/index.js`, and `scripts/prepare-sites-build.mjs` as protected runtime files. Do not edit, replace, remove, or recreate them unless the user explicitly asks to change the mobile runtime itself. For an explicit runtime change, update the affected lock hashes only after verifying the new runtime behavior.
- Run `npm run check:runtime` before preview or handoff. If it fails, restore the protected runtime instead of weakening or bypassing the check.
- `npm run build` preserves the mobile runtime and prepares the static Cloudflare Worker output required by Sites. Before a Sites handoff, confirm `dist/client/index.html`, `dist/server/index.js`, `dist/.openai/hosting.json`, and source `.openai/hosting.json` exist, then run `npm run test:sites`. Do not replace this project with a Vinext starter.

## Runtime Contract

- Preserve the mobile device runtime unless the user's task explicitly asks otherwise. Do not replace it with a standalone page. Visual fidelity applies to app-owned content inside the device screen, not to template-owned device chrome.
- Keep `App` composed around `PhoneFrame` -> `KeyboardProvider`, with `StatusBar`, app content, `HomeIndicator`, and `KeyboardDock` mounted inside the phone frame. `StatusBar` and the iOS home indicator are overlaid device chrome. When the Android keyboard is closed, the app viewport reserves the protected navigation-bar region instead of painting behind it. When the Android keyboard is open, preserve the current full-screen keyboard layout: its asset includes the IME navigation strip and the separate black navigation bar is hidden. iOS screens continue to paint behind the home-indicator area and own their safe-area content padding.
- Preserve the `iPhone` / `Pixel 10` device picker and both calibrated device presets. The Pixel screen is `427 x 952`; its `32 x 32` camera circle and `public/assets/android/navigation-bar.svg` bottom navigation bar are protected device chrome, not app content.
- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. Keep the prototype root and default app screen white.
- Preserve `StatusBar` as live device chrome, including its platform-specific typography, source status-icon assets, and spacing. Pixel 10 uses Roboto, Android indicators, and 32px top, left, and right padding. iPhone uses its iOS indicators, system typography, and calibrated spacing. Do not hardcode screenshot times like `9:41` into the status bar, replace its real-time clock, or move status bar content into app markup unless the user explicitly asks for a fixed/mock device time.
- `PhoneFrame` owns the calibrated device frame, screen portal, device picker, camera cutout, and custom cursor. Keep device assets in `public/assets/iphone/` and `public/assets/android/`; if an asset fails to load, repair the asset path or restore the asset instead of removing the frame, keyboard, or image render.
- Use `MobileScroll` directly for simple single-screen prototypes. Use `FlowStack` for conventional multi-screen flows whose routes can own their fixed header and footer; when using it, define each route as a `FlowScreen`: `{ id, header?, headerHeight?, footer?, footerHeight?, render }`, and use `flow.push(screen)`, `flow.pop()`, and `flow.replace(screen)` from `FlowStack` render callbacks or `useFlow()` instead of introducing another router.
- Use `Carousel` for a carousel, horizontal rail, swipeable cards, image or media strip, horizontally scrollable cards, chip rail, or other horizontal collection.
- For a layered app shell—such as a persistent composer, independently presented sheet, pushed/peek sidebar, or app-wide transition—compose directly in `Prototype.tsx` rather than forcing it through `FlowStack`. Keep app-owned fixed chrome as sibling layers outside `MobileScroll`.
- When using `FlowScreen`, put route-owned fixed headers or footers in `FlowScreen.header` or `FlowScreen.footer`. Set `headerHeight` to the visible app-toolbar height; `FlowStack` adds the device's top safe-area/status-bar inset automatically. Do not include `StatusBar` or its height in the header. Set `footerHeight` to the full app-footer height. `FlowScreen.footer` is an overlay, not reserved layout space; screens using it must add their own bottom content padding such as `padding-bottom: calc(var(--flow-footer-height) + var(--mobile-safe-area-height) + 24px)` so final content can scroll above the footer while still painting behind it.
- Render only scrollable content inside `MobileScroll`; it is for content that should move with scroll and rubber-band overscroll. Keep app-owned headers, nav bars, tabs, composers, and overlays outside it. This keeps scroll physics, safe areas, keyboard insets, scrollbars, and drag click suppression active without letting content paint under fixed chrome.
- Buttons, links, cards, and images inside `MobileScroll` should still allow drag scrolling when the pointer moves beyond tap slop. Use `data-scroll-drag="ignore"` only for rare controls that must own the drag gesture themselves.
- Do not add `var(--keyboard-height)` to ordinary screen/content padding inside `MobileScroll`; the scroll viewport already shrinks above the simulated keyboard. For custom fixed composers, search bars, or toast chrome, use `useKeyboardInsets().bottomInset`. It is relative to the app viewport: Android returns `0` while the closed-keyboard viewport already reserves navigation, then returns the keyboard height while open; iOS continues to clear the home indicator while closed and ride directly above the keyboard while open. Do not pin custom bottom chrome to `bottom: 0` or only `keyboardHeight`.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for every text-entry control. A raw `input` or `textarea` disconnects focus, keyboard animation, safe-area insets, and attached surfaces.
- Use `BottomSheet` for phone-scoped sheets. Its props are `open`, `onOpenChange`, `title`, optional `description`, optional `snap`, and `children`; it renders through the phone screen portal and dismisses the keyboard before opening.

## Horizontal Carousels

- Use `Carousel` for horizontally draggable cards, images, media, chips, or other horizontal collections. Do not recreate these with `overflow-x`, custom pointer handlers, or a generic div.
- `Carousel` can be nested directly inside `MobileScroll`. It owns horizontal gestures and automatically yields vertical gestures to the parent.
- Never put `data-scroll-drag="ignore"` on or around a `Carousel`; doing so prevents vertical parent scrolling when a gesture begins inside it.
- Do not add CSS scroll snapping to `Carousel`; its runtime owns momentum and release motion.
- Use `data-scroll-drag="ignore"` only when a control must prevent parent scrolling in every drag direction.

See `src/mobile/COMPONENTS.md` for the full component and gesture contract.

## PostgreSQL and Identity Boundary

- PostgreSQL is the only supported application database. Runtime connections come from `DATABASE_URL`; API tests require a separate, non-production `TEST_DATABASE_URL` and run against randomly named schemas in a real PostgreSQL database. Do not reintroduce an embedded or local-file database fallback, and never point tests at production.
- Keep migrations and seeds asynchronous and repeatable. Multi-write business transitions must use `AppDatabase.transaction` and every statement in the callback must use the provided transaction database so the work remains on one PostgreSQL connection. Preserve required row locks, conditional updates, unique constraints, partial indexes, and idempotency rules when changing capacity, inventory, payment, redemption, or status flows.
- `users` is the canonical internal principal. `user_identities` maps a verified external identity by `provider + provider_app_id + provider_subject`, with an optional server-verified `union_subject`; `user_sessions` stores only hashed Bearer tokens plus expiry, revocation, and last-seen state. `provider_app_id` must stay in the identity uniqueness boundary so equal-looking subjects from different mini-program AppIDs cannot collide.
- A client must never submit a trusted `user_id`, `openid`, or `unionid`. Resolve owner identity with `requireCurrentUser`, then apply that server-derived internal `user_id` to every owner vehicle, quote, booking, order, idempotency, and media query. Resource IDs in the request are selectors, not proof of ownership.
- The invisible Web development fallback is permitted only when `YUXIAOMAN_ALLOW_DEMO_AUTH_FALLBACK=true` in a non-production environment; production ignores it and requires a valid server-issued Bearer session. The lack of a visible login page does not permit anonymous owner data access. Keep the explicit development-session endpoint non-production-only and separately gated.
- The native WeChat production path is future work and must be server-mediated: `wx.login()` returns a one-time `code`; the client sends only that code to the API; the server uses the AppSecret with `code2Session`, verifies `openid`/optional `unionid`, maps the identity to an internal `user_id`, and issues the application session. AppSecret and trusted WeChat identifiers never belong in client code or client-authored identity fields.

## Car Insurance Lead-Matching Boundary

- Product decision (2026-08-16): the Web MVP may add a dedicated car-insurance renewal lead flow as a second closed service journey while keeping annual inspection as the primary home journey. The selected visual source of truth is `references/insurance-lead-selected.png`.
- Position this flow only as “续保需求登记与专业人员对接”. 驭小满 does not quote, compare, recommend, underwrite, sell, bind, collect premiums, or expose an advisor phone number in this module.
- Keep the owner flow intentionally short: select an existing vehicle, enter the approved minimum contact/renewal fields, upload one driving-license main-page image, grant explicit unchecked-by-default disclosure consent, then receive an immutable service number and withdrawal capability. Do not add a customer-facing progress timeline.
- Keep insurance leads independent from annual-inspection bookings, stations, time slots, and payment. The operator flow may view sensitive lead details, copy a handoff summary, record the named recipient and time, and close the lead; every sensitive read and state change must be audited.
- Product decision (2026-08-24): the insurance-lead area has no module-specific credential, but it is platform-admin-only behind the unified named backoffice session. A wash-store administrator must receive `403` before any insurance list, detail, media, disclosure, handoff, or close data is read.
- Product decision (2026-08-20): the insurance disclosure editor must keep a default-collapsed “客户端实时预览” guide with one exact 1–8 vocabulary shared by the upper field labels, live-preview markers, and lower legend: 1 接收合作方, 2 资料接收主体, 3 授权资料范围, 4 使用目的, 5 保存期限, 6 预计联系时效, 7 用户授权文案, and 8 披露版本. The preview must render from the current normalized form draft rather than a static screenshot, so upper edits immediately appear at the matching client locations; pins 1 and 2 share the client's combined “信息接收方” row but remain separately identified. Describe it as the client effect after saving, keep version 8 visible even after a save-success message, show that an edited draft receives a new server-generated version only after saving, and preserve the explanation that stale open pages must reread and reauthorize.
- Real-data handling is fail-closed: without HTTPS, an application data key, a separate phone HMAC key, and a named configured recipient, the insurance flow must remain in synthetic demo mode. The data key also signs owner withdrawal capabilities. Contact data is encrypted, uploaded media stays outside public assets, and user-facing/admin-list responses remain masked by default.

## Car Wash Closed-Loop Boundary

- Product decision (2026-08-16): car wash is an additional owner-service journey and must remain independent from the annual-inspection booking and fulfillment state machine. Preserve annual inspection as the primary journey and use additive DTO discrimination: annual inspection returns `serviceType: "annual_inspection"`; car wash returns `serviceType: "car_wash"`.
- The implemented loop uses the owner-facing consumer flow plus the existing shared desktop backoffice. The backoffice now has two server-authorized roles: platform administrators and one invited administrator per wash store. Do not create a separate merchant app, multi-store account, custom role editor, merchant wallet, or payout surface. A wash-store administrator may see only its own dashboard, orders, redemption workflow, future slots, store-owned profile/gallery fields, store sale prices, read-only reconciliation, and its own audit events; cancellation, refund, settlement mutation, global package maintenance, platform ranking, and every non-wash module remain platform-only.
- Preserve the owner flow: select an existing vehicle and its canonical `sedan`, `suv`, or `mpv` wash price category → choose a clearly labelled demo store, package, and capacity-backed slot → obtain a 10-minute immutable quote → create a `pending_payment` order with a 15-minute capacity hold → confirm the explicitly labelled local `mock` payment → receive a unique six-digit redemption code → present the code at the store → operations manually redeems it → operations records the offline settlement amount and note. Vehicle maintenance must expose all three categories, and consumer pricing must come from the selected vehicle's persisted category rather than a client-authored override. On the booking screen, visibly bind package and service-fee prices to both the selected plate and its Chinese category label so a driver cannot mistake them for a universal price. Treat legacy `suv_mpv` only as a migration/read-compatibility value; never create a new combined-category offer or vehicle.
- Product decision (2026-08-19): wash booking supports both `self_drive` and `valet`. Wash valet is a same-address round trip: operations coordinates an offline driver to collect the vehicle, deliver it to the selected wash store, and return it to the original pickup address. Price it only from the pickup point to the store using the Tencent one-way driving route; the return leg is included and is never charged a second time. A wash store may inherit the existing global valet rule or wholly override it through `wash_store_valet_pricing_overrides`. If a real Tencent route is unavailable, out of range, or the rule is missing, block the valet quote and offer self-drive instead; never price valet from an estimated distance.
- A wash slot remains the store's wash-service window, not a guaranteed driver pickup appointment. The owner UI must say that the exact collection time is confirmed offline. Do not add driver-arranged, picked-up, returning, or other driver workflow states to the six-state wash order machine, and do not imply live driver tracking.
- Product decision (2026-08-22): choose a wash store from a dedicated information-rich list comparable to the annual-inspection station picker, rather than a compact selector inside booking. Public store DTOs expose the complete owner-safe description, tags, facilities, hours, rating, starting price, cover image, and ordered gallery. Operations maintains those text fields and at most 20 gallery images per store, with exactly one cover whenever images exist. Only the gallery endpoints may change the managed cover; ordinary store-detail writes preserve it and ignore client-authored cover URLs. Uploaded images are decoded, metadata-stripped, resized, and re-encoded by the server; public DTOs and media never expose internal store contacts or storage keys, and public image URLs for disabled stores return 404 while the internal admin may still inspect them. Synthetic store/image data stays visibly marked as demo.
- The existing six-digit redemption contract remains the only fulfillment proof. Self-drive owners show it at the store; valet owners show it during vehicle handoff so operations can complete the store-side manual redemption offline. Do not add a driver app, merchant app, or a second redemption state machine for valet.
- A redemption code must not exist before confirmed mock payment. Payment moves an order to `awaiting_redemption` and creates the code; cancellation or refund makes it inactive. Redeeming the same paid order repeatedly is idempotent. Platform invalid/inactive codes use a controlled conflict response; store accounts must receive the same `404` for another store's code, an unknown code, or an inactive code, and repeated failures are database-rate-limited by the authenticated account, fixed store, and source IP. Manual redemption must audit `source` (`wechat`, `phone`, or `other`), the server-resolved operator, and optional `note` in `wash_order_events` without persisting the submitted code in audit metadata.
- Keep wash order states limited to `pending_payment | awaiting_redemption | redeemed | cancelled | refunded | expired`. Keep settlement states limited to `unsettled | settled | void`. Only a `redeemed` order can be marked `settled`; changing an already settled record requires a correction reason.
- “Settlement” is only an operations record of an offline reconciliation. This MVP does not perform payment clearing, commissions, automatic split settlement, merchant wallets, withdrawals, invoices, or bank/payment-provider transfers. Never describe the mock charge/refund or settlement entry as real money movement.
- Capacity, quote expiry, ordering, payment, rescheduling, cancellation/refund, redemption, and settlement rules belong to the server. Use transactional capacity checks and idempotency keys; never let an optimistic client transition bypass the canonical order state.
- Canonical consumer APIs are `/api/wash/stores`, `/api/wash/stores/:id`, `/api/wash/store-images/:id`, `/api/wash/stores/:id/offers`, `/api/wash/stores/:id/slots`, `/api/wash/quotes`, `/api/wash/orders`, `/api/wash/orders/:id`, and the order sub-actions `/payments`, `/cancel`, and `/reschedule`. Canonical operations APIs are under `/api/admin/wash`: provider dashboard, store CRUD and store-image upload/order/cover/delete, package CRUD, per-store offers, slots and `/slots/batch`, order query/detail, `/orders/redeem`, read-only settlement query, controlled order `PATCH`, and platform-only order settlement `PUT`.
- Never trust client-authored wash pricing facts. The server derives `wash_vehicle_category` from the owner vehicle, public store DTOs omit internal contacts, and valet pickup POI text/coordinates must carry a short-lived server-signed `locationProof` (or pass the explicitly gated non-production demo resolver) before Tencent route pricing. Production must reject unsigned/demo pickup coordinates.
- Every `/api/admin/wash/*` route requires the unified backoffice session. Platform administrators may use the full maintenance surface; wash-store administrators are constrained to the capabilities and single store returned by the session. Cross-store resource IDs return `404`, missing capabilities return `403`, and every write plus denied scope attempt records the server-resolved actor. Provider order DTOs must omit owner name/phone, valet pickup address, redemption code, platform-only notes, and other nonessential private fields. Pagination for provider orders, slots, and settlements must apply the fixed-store predicate before both item and total queries; a client `storeId` may narrow platform scope but never broaden provider scope. Continue to enforce payload validation, canonical order transitions, idempotency, and pickup-place tamper protection through `WASH_LOCATION_PROOF_SECRET` in production.
- Preserve the independent PostgreSQL domain entities `wash_stores`, `wash_store_images`, `wash_packages`, `wash_store_offers`, `wash_slots`, `wash_quote_snapshots`, `wash_orders`, `wash_order_events`, `wash_order_payments`, and `wash_order_settlements`, plus nullable `vehicles.wash_vehicle_category`. Migrations must remain additive and repeatable within the existing annual-inspection PostgreSQL schema.
- `wash-seed-v1` is synthetic demo data: three stores must remain visibly labelled “演示”; standard/detailing prices are 3800/8800 fen for sedans, 4800/10800 fen for SUVs, and 5800/12800 fen for MPVs. Operations may change all three categories separately. Do not present these stores, ratings, slots, or prices as live partners or production inventory.
- For wash work, run `npm run api:test` and keep the wash catalog/quote/order/payment/redemption/settlement and capacity/expiry/reschedule/refund/admin-maintenance cases passing alongside every annual-inspection regression. Run `npm run admin:test` for the operations UI, and manually verify “车主服务 → 洗车预约” plus the admin navigation entries “洗车订单 / 洗车门店 / 洗车套餐与价格”.
- Inspection-station and wash-store coordinates are system data, not operator input. New locations must be created from a signed `/api/locations/suggestions` candidate; editing an existing station/store preserves its saved location unless the operator explicitly selects a new candidate. Keep address/district/latitude/longitude read-only in the admin UI, never expose an editable POI ID or fall back to a default map-center coordinate, and derive persisted address/district/coordinates/POI from the verified `location` object on the server.

## Car-Rental Boundary

- Product decision (2026-08-22): the car-rental product is delivered only through the native WeChat mini-program, the operations admin, and their shared Fastify/PostgreSQL API. The React/Vite interview MVP is not a rental product surface and is excluded from rental implementation and acceptance. The native rental visual source of truth is `references/car-rental-search-selected.png`; preserve its search-first hierarchy, solid cobalt/white palette, transparent price summary, exact-model cards, and compact mobile density.
- Preserve the journey `location and time -> exact rental model -> server quote -> order -> mock payment -> fulfillment`. Support only same-store pickup/return (`store_pickup`) and same-address delivery/collection (`home_delivery`); do not offer different-store or one-way return. The customer chooses an exact model while operations assigns the physical fleet vehicle.
- Rental periods are 1–30 billable days, require at least two hours' advance notice, and use `ceil(duration / 24h)` in `Asia/Shanghai`. A pending-payment order holds model/store capacity for 15 minutes. Canonical states are `pending_payment -> confirmed -> ready_for_pickup -> in_use -> return_pending -> completed`, with `cancelled` and `expired` as terminal side paths.
- Never trust client-authored rental prices. The server calculates the frozen quote from daily date rates, ¥60/day basic protection, ¥35/order preparation, optional ¥60/day enhanced protection, and any delivery fee. Vehicle and violation deposits are separate simulated pre-authorizations and are never included in payable total. Weekend rates default to +15%, while an exact-date override takes precedence.
- Home delivery requires a trustworthy driving-route result. The configured demonstration rule is ¥29 including 3 km one way, then ¥6 for each started kilometre beyond that, with a 20 km radius. Never substitute straight-line distance or an estimate when route pricing is unavailable; block the delivery quote and keep store pickup available.
- Keep the new PostgreSQL rental domain additive and independent under `car_rental_*`: brands/models, stores, fleet vehicles/images, rates/overrides, policy versions, quote snapshots, orders/events, adjustments, and mock payments. Keep legacy `used_car_*` records read-only for historical compatibility; never migrate a sales listing into fleet inventory automatically. Fleet vehicles referenced by history may be retired but not hard-deleted.
- Seed and UI data are visibly synthetic demonstrations: 15 brands, 46 exact models with the existing licensed and traceable real-model reference photos, three Tianjin demo stores, and 36 synthetic fleet vehicles (30 active, 3 maintenance, 2 offline, 1 retired). Photos identify the model only; color and the delivered physical vehicle remain subject to the assigned demo fleet record.
- Operations exposes five rental work areas: brands/models, stores, fleet, rates, and orders. It may configure store location/hours, model specs/photos, fleet status, weekday/weekend/date rates, protection/deposit/policy rules, delivery radius/fees, assignment, controlled lifecycle transitions, adjustments, notes, and event audit.
- Canonical public APIs are `/api/car-rental/catalog`, `/api/car-rental/stores`, `/api/car-rental/offers/search`, `/api/car-rental/models/:id`, `/api/car-rental/quotes`, `/api/car-rental/orders`, `/api/car-rental/orders/:id`, and the order sub-actions `/mock-pay` and `/cancel`; canonical maintenance APIs live under `/api/admin/car-rental`.
- This remains a presentation-stage company-operated demo. Do not imply real payment, credit-based deposit waivers, insurance underwriting, document OCR, vehicle telematics, remote unlock, traffic-violation lookup, or production inventory integration.

## Driving-School Directory Boundary

- Product decision (2026-08-22): driving-school service is delivered only through the native WeChat mini-program, the operations admin, and their shared Fastify/PostgreSQL API. It is a Tianjin-first directory and platform-consultation flow: school list -> eligibility/training-class filters -> school details and reference offers -> user-initiated phone/navigation or a revocable inquiry receipt. Do not add online enrollment, payment, orders, a driving-school merchant surface, or any React/Vite MVP implementation.
- Treat the official driving-license class and the school's regulatory record as separate facts. Display the exact code plus official name for A1/A2/A3, B1/B2, C1-C6, D/E/F, and M/N/P; distinguish initial application from additional-class application and return any conditions from the server. Never relabel these as vague “A证/B证/C证”. A filed school's level 1/2/3 training capacity indicates the number of supported vehicle categories, not teaching quality or a rating.
- Keep regulatory data, official quality data, and school-supplied commercial information visibly separate. Real schools may be published only while their filing or legacy-license record is verified and valid. Synthetic schools and their prices must always say “演示数据”; never show a “12123认证” badge, claim nationwide 12123 marketplace equivalence, or imply live government synchronization. Do not show pass rates, violation rates, accident rates, or rankings without an authoritative source and explicit statistical period.
- Driving-school offers are reference information supplied by the school and the final amount is governed by the offline contract. Preserve `fixed`, `starting_from`, `range`, and `inquiry` price modes in integer fen together with unit, applicable training class/application mode, inclusions, exclusions, and an explicit active/inactive status. Product decision (2026-08-24): driving-school offers do not expire; legacy validity-date columns are compatibility-only and must not control public visibility, minimum-price calculation, inquiry eligibility, or operations UI. Operations changes or hides a price by editing or disabling the offer.
- Product decision (2026-08-24): driving-school maintenance is school-centric. The operations module has a school directory and global inquiry list; pricing is maintained only inside the selected school's full-page workbench. The workbench must keep the school identity visible, provide saved-data-only mini-program list/detail previews plus the server-derived publish checklist, and make every editable field's mini-program destination obvious. Do not restore an independent cross-school offer editor.
- Product decision (2026-08-24): `openHours` is the only operations-maintained and owner-visible driving-school business-hours summary. Do not add a weekly schedule editor or overwrite legacy weekly-schedule data when saving unrelated school fields.
- The owner may call only a school's published admissions phone through an explicit `wx.makePhoneCall`, or submit a separate inquiry to “驭小满驾校服务团队”. Platform inquiries are not forwarded or exported to a school. Consent is unchecked by default; encrypt contact name and phone, deduplicate with a separate phone HMAC, mask list responses, audit sensitive reads and state changes, and fail closed for real data when the production privacy configuration is incomplete.
- Inquiry states are `new -> contacting -> resolved | closed`; the owner may irreversibly withdraw a non-terminal inquiry to `withdrawn`. A withdrawn inquiry cannot be contacted, restored, or changed by operations. Repeated submissions by the same owner for the same school, class, and phone within 24 hours return the original receipt and withdrawal capability.
- New physical school locations must come from a signed `/api/locations/suggestions` candidate. Public DTOs may expose only the published admissions contact and verified location; internal contacts remain operations-only. Driving-school maintenance remains platform-admin-only behind the unified backoffice session and has no separate module password.

## Keyboard Rule

The simulated keyboard is a separate top-layer component. Before presenting anything that behaves like iOS navigation or modal UI, dismiss it first.

Call `keyboard.hide()` before:

- pushing, popping, or replacing FlowStack routes
- opening bottom sheets, action sheets, dialogs, menus, or navigation sheets
- starting transitions where the destination should not inherit text-input focus

`FlowStack` already hides the keyboard for `push`, `pop`, and `replace`. `BottomSheet` already hides it before opening. If you add new modal/sheet/navigation primitives, follow the same rule.

When a composer, search surface, or other keyboard-attached component closes, call `keyboard.hide()` in the same event before changing that component's open state. Position attached surfaces from `useKeyboardInsets()` rather than a separate timer or visibility flag so both dismiss together.

When any text-entry control loses focus, dismiss the simulated keyboard. If the control is custom or does not use the runtime's keyboard-aware fields, handle its blur event and call `keyboard.hide()` explicitly. Keep the keyboard open only when focus is moving directly to another text-entry control that should share the same keyboard session.

## Interaction Rules

- Do not trigger buttons or inputs after a pointer has become a drag. Preserve the drag suppression behavior in `MobileScroll`.
- Do not allow native browser image/file dragging inside the phone frame. Preserve the phone-level `dragstart` suppression and non-draggable image styles so scroll drags that begin on images still scroll the prototype.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for text entry so the simulated keyboard and safe-area insets stay connected.
- Fixed phone chrome should not animate with pushed screens. Screen content can animate; the status bar, camera cutout, and preview chrome should stay put.
- Keep the keyboard below the home indicator/safe area layer in z-index, and above ordinary app UI while visible.
- Keep the home indicator as the topmost safe-area layer in the z-index above everything else in the prototype.
